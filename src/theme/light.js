// Descriptive Colors
// const white = '#fff';
const black = '#000';
const blue = '#243961';
const gold = '#e4c88e';
const grayLight = '#fcfcfc';

// Functional Colors
const ErrorMessage = {
  backgroundColor: grayLight,
  padding: '1em',
};

const LightTheme = {
  backgroundColor: grayLight,
  primary: blue,
  secondary: gold,
  textColor: black,
  borderColor: grayLight,

  ErrorMessage,
};

export default LightTheme;
