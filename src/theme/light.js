const white = '#fff';
const black = '#000';
const blue = '#243961';
const gold = '#E4C88E';
const grayLight = '#fcfcfc';

const LightTheme = {
  backgroundColor: grayLight,
  primary: blue,
  secondary: gold,
  textColor: black,
  borderColor: grayLight,
};

export default LightTheme;
